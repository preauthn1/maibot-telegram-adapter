# MaiBot 语言指纹审计（2026-09-14）

## 结论

当前实例已经有“去客服腔”处理，但它把不同账号、不同群的输出压到相同的低方差形式：短句、无句号、少 emoji、删连接词、常见附和词。它降低了显性 AI 套话，却形成了可识别的 MaiBot 共同风格。

真正需要解决的是**跨场景输出同质化**，不是再追加一张禁词表。

## 已核实的运行事实

- `maisaka_replyer.prompt` 只有一条通用生成指令：`日常且口语化的回复`。
- 所有聊天共用 `[personality].reply_style` 与出站 `humanize.py`。
- `humanize.py` 会统一删除/替换书面连接词、客服腔、书面标点、句尾句号，并把 emoji 限为最多一个。
- 该实例的 `multiple_probability = 0.0`，备用风格完全没有参与运行。
- 回复模型使用 `sequential` 策略，优先固定为 `deepseek-v4-flash`。
- 账号历史已经存在于 `data/plugins/preauthn1.telegram-user-adapter/transcripts/`：12 个聊天流、2588 条去重出站样本。

## 语料统计

统计基于 Telegram 账号 `<本部署账号ID>` 的出站消息；按聊天流与 message_id 去重。

- 2588 条样本，覆盖 12 个聊天流。
- 长度分布：1–2 字 101 条；3–6 字 511 条；7–12 字 1076 条；13–30 字 763 条；31 字以上 137 条。
- 仅 1 条以句号结尾；问号结尾 16 条。
- 高频开头：`确实` 23 次、`正常` 10 次、`笑死` 9 次、`哈哈` 7 次、`懂了` 5 次。
- 同一精确短句跨群复用：`笑死` 出现在 4 个群；`正常`、`啊这个就别问了`、`问这个干嘛` 等出现在多个群。

这些统计并不证明“机器人”，但它们解释了为什么熟悉 MaiBot 的人能通过**低方差短句 + 统一清洗痕迹 + 跨群复用**建立直觉判断。

## 当前机制的两个矛盾

1. **账号画像与清洗器冲突**
   - 账号历史画像明确记录“偶尔只发一个 emoji 当回应”。
   - 出站器当前无条件跳过纯 emoji / 纯标点回复。
   - 这不是安全约束，而是把已验证的账号习惯消掉，换成所有部署通用的规则。

2. **全局风格与场景风格冲突**
   - 已积累的画像是全账号级别，不能区分技术群、熟人群、主题群和私聊。
   - 同一人格与同一改写器用于所有聊天流，导致群间语言特征过度一致。

## 已实施（当前仓库）

- 已新增 `style_profiles.py`：只从该部署账号自身的本地 transcript 推导控制，不共享账号、语料或配置。
- 已新增 `scripts/generate_style_profiles.py`：按聊天流生成画像；少于 20 条去重本人消息的聊天流不启用风格覆盖。
- 画像卡新增并热加载 `style_enabled`、`allow_emoji_only`、`max_emoji`、`preserve_trailing_period`、`style_max_chars`；后者由该聊天流的本人消息 P90 推导。
- 出站器按聊天流应用上述控制；若同时存在人工风险 `max_chars`，两者取更严格的值，绝不自动放宽。
- 已新增 `reply_reuse_guard.py`：同一短泛化回复在 30 分钟窗口内已经发往另一个聊天流、或在同一聊天流 10 分钟内复读时，拒绝发送而非同义词轮换。
- 已新增 `style_metrics.py` 与 `scripts/style_metrics_report.py`：可对每个部署自己的 transcript 生成无原文指标快照，追踪跨群复用率、长度熵和高频开头；适配器协议头与附件占位符会被排除，避免污染基线。
- 画像在服务启动时立即生成，并每小时仅用本部署本地 transcript 自动刷新；停止插件时会取消刷新任务。
- 画像与指标均排除适配器协议头和附件占位符，避免内部格式污染长度、首词及复用基线。
- replyer 主程序现会在生成前读取**当前 Telegram 聊天流**的画像卡，向 system prompt 注入其长度、句号和 emoji 边界；不会读取其他群或全局画像。

当前数据生成结果：8 个样本充足的聊天流已写入独立画像卡；4 个样本不足的聊天流未启用覆盖。

## 后续改造顺序

### 1. 先做“按聊天流画像”，不要先改模型

从每个聊天流中仅取账号已发送的去重消息，生成可审计的风格卡：

- 长度分布、分句率、问号/emoji/口头禅分布；
- 可接受的纯反应（例如 `😂`）及其出现条件；
- 常见回应动作（附和、追问、纠错、沉默）；
- 禁止迁移到其他群的词和梗；
- 样本数不足时不启用该群画像，不能拿全局画像伪造局部习惯。

画像应作为**生成前约束**进入 replyer，而不是事后做字符串删改。

### 2. 把 `humanize.py` 从全局规范改为“最小安全过滤”

保留：工具标记泄漏、污染输出、NSFW、身份矛盾、虚构命令等安全处理。

移出全局硬规则：句号删除、纯 emoji 禁止、统一替换连接词、统一 emoji 上限。它们应改由每个聊天流的画像决定；没有画像就宁可不发，不要假装有个人化风格。

### 3. 引入输出多样性预算

每个聊天流记录最近 N 条机器人自身的：

- 首词；
- 精确短句；
- 回复动作；
- 长度桶；
- 引用回复比例。

若候选与近期输出重复、或与其他群同时间窗的输出高度相似，优先重写为有新信息的版本；若没有自然替代，则不发。不能用同义词轮换来“伪随机”，那会制造新的规律。

### 4. 回复策略改为“证据不足则沉默”

目前最显眼的风险不是用词，而是任何话题都能给出顺滑、即时、完整的回应。对没有明确上下文、没有个人经历或没有可靠事实依据的话题，Planner 应优先 `wait`，而不是生成泛化的“确实/正常/笑死”。

### 5. 建立回归评测，而非主观感觉

离线回放一批历史上下文，比较改造前后：

- 跨群精确复用率；
- 最近 50 条中的首词重复率；
- 回复长度熵；
- 无上下文泛化附和率；
- 被真人明确质疑后的复发率；
- 事实、身份和安全拦截不能退化。

## 最小可行实现

不修改 MaiBot 主程序。新建一个独立插件，使用现有 hook：

1. 读取本账号已授权的本地 transcript；
2. 为样本足够的聊天流建立只读风格画像；
3. 在 `maisaka.reply.before_post_process` 比较候选与该流近期输出；
4. 仅对重复度过高的候选请求一次带画像的重写，或拒绝发送；
5. 写入可审计日志，记录命中的“重复”依据，但不存或输出聊天原文。

这比改动主程序风险小，且可先在当前白名单群以观测模式运行。观测模式只记录分数，不改写或拦截，验证阈值后再启用。

## 需要确认

实施需要新增独立插件，并访问既有本地 transcript（不上传、不外发）。项目约定要求：插件可独立开发；若改动主程序，必须先获得明确许可。
